export function envSelectedTab(state) {
    return state.selectedTab;
}

export function envSelectedMap(state) {
    return state.selectedMap;
}

export function envTabList(state) {
    return state.tabList;
}

export function envHeadMap(state) {
    return state.headMap;
}

export function envHintMsg(state) {
    return state.hintMsg;
}

export function envMerchantId(state) {
    return state.merchantId;
}

export function envIsPC(state) {
    return state.isPC;
}
